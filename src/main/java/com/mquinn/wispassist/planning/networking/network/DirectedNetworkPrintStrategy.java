package main.java.com.mquinn.wispassist.planning.networking.network;

public class DirectedNetworkPrintStrategy extends GeneralNetworkPrintStrategy implements INetworkPrintStrategy {

    /**
     * Interface for directed graph network printing classes
     *
     * @param network
     */
    @Override
    public void printNetwork(Network network) {
        super.printNetwork(network);
    }

}
