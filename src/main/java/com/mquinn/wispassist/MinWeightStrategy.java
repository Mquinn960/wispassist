package main.java.com.mquinn.wispassist;

import main.java.com.mquinn.graphing.Edge;

public class MinWeightStrategy implements IWeightStrategy {

    @Override
    public double calculateEdgeWeight(Edge edge) {
        return 0;
    }
}
